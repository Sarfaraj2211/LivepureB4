1) git pull ---> to get latest code from the repository
2) git add .    ---->add all file which need to push on repo
3) git commit -m "message"

ex:-
git commit -m "created registration and login page"

4) git push -----> to push our code changes from local to git repo